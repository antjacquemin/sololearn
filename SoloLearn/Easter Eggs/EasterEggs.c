#include <stdio.h>

int main() {
    int eggs, eggsBasket, eggsFriend;
    scanf("%d", &eggs);
    scanf("%d", &eggsBasket);
    scanf("%d", &eggsFriend);
    if (eggs = eggsBasket + eggsFriend)
        printf("Candy Time");
    else 
        printf("Keep Hunting");
    return 0;
} 