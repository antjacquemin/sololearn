#include <iostream>
using namespace std;

int main() {
    int pesos, dollars;
    cin >> colors >> dollars;
    if (dollars > pesos / 50)
        cout << "Pesos";
    else
        cout << "Dollars";
    return 0;
}